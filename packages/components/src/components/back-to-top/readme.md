# t-back-to-top



<!-- Auto Generated Below -->


## Properties

| Property           | Attribute           | Description | Type     | Default |
| ------------------ | ------------------- | ----------- | -------- | ------- |
| `visibilityHeight` | `visibility-height` |             | `number` | `200`   |


## CSS Custom Properties

| Name                                               | Description                                      |
| -------------------------------------------------- | ------------------------------------------------ |
| `--back-to-top--container-background`              | Background of back to top container              |
| `--back-to-top--container-border-radius`           | Border radius of back to top container           |
| `--back-to-top--container-bottom`                  | Bottom of back to top container                  |
| `--back-to-top--container-cursor`                  | Cursor of back to top container                  |
| `--back-to-top--container-display`                 | Display of back to top container                 |
| `--back-to-top--container-height`                  | Height of back to top container                  |
| `--back-to-top--container-position`                | Position of back to top container                |
| `--back-to-top--container-right`                   | Right of back to top container                   |
| `--back-to-top--container-visible-after-color`     | Color of back to top container visible after     |
| `--back-to-top--container-visible-after-content`   | Content of back to top container visible after   |
| `--back-to-top--container-visible-align-items`     | Align items of back to top container visible     |
| `--back-to-top--container-visible-display`         | Display of back to top container visible         |
| `--back-to-top--container-visible-justify-content` | Justify content of back to top container visible |
| `--back-to-top--container-width`                   | Width of back to top container                   |
| `--back-to-top--container-z-index`                 | Z index of back to top container                 |


----------------------------------------------

*Built with [StencilJS](https://stenciljs.com/)*
