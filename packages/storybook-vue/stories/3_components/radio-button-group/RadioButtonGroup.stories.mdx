import {
  Meta,
  Story,
  ArgsTable,
  Canvas,
  Description,
} from '@storybook/addon-docs/blocks';
import ScaleRadioButtonGroup from './ScaleRadioButtonGroup.vue';
import { action } from '@storybook/addon-actions';

<Meta
  title="Beta Components/Radio Button Group"
  component={ScaleRadioButtonGroup}
  argTypes={{}}
/>

export const StandardTemplate = (args, { argTypes }) => ({
  props: {
    width: { type: String },
    ...ScaleRadioButtonGroup.props,
  },
  computed: {
    wrapperStyles() {
      return {
        maxWidth: this.width,
      };
    },
  },
  template: `
    <div :style="wrapperStyles">
    <scale-radio-button-group :label="label" :status="status" :helper-text="helperText">
      <scale-radio-button
        value="1"
        label="Radio Button"
        name="radioStandard"
        transparent="true"
        input-id="choiceOne"
      ></scale-radio-button>
      <scale-radio-button
        value="2"
        label="Radio Button"
        name="radioStandard"
        input-id="choiceTwo"
      ></scale-radio-button>
    </scale-radio-button-group>
    </div>
  `,
});

export const HelperTextTemplate = (args, { argTypes }) => ({
  props: {
    width: { type: String },
    ...ScaleRadioButtonGroup.props,
  },
  computed: {
    wrapperStyles() {
      return {
        maxWidth: this.width,
      };
    },
  },
  template: `
    <div :style="wrapperStyles">
    <scale-radio-button-group :label="label" :status="status" :helper-text="helperText">
      <scale-radio-button
        value="1"
        label="Radio Button"
        name="RadioButtonHelperText"
        transparent="true"
        input-id="choiceOne"
      ></scale-radio-button>
      <scale-radio-button
        value="2"
        label="Radio Button"
        name="RadioButtonHelperText"
        input-id="choiceTwo"
      ></scale-radio-button>
    </scale-radio-button-group>
    </div>
  `,
});

export const ErrorTemplate = (args, { argTypes }) => ({
  props: {
    width: { type: String },
    ...ScaleRadioButtonGroup.props,
  },
  computed: {
    wrapperStyles() {
      return {
        maxWidth: this.width,
      };
    },
  },
  template: `
    <div :style="wrapperStyles">
    <scale-radio-button-group :label="label" :status="status" :helper-text="helperText">
      <scale-radio-button
        status="error"
        value="1"
        label="Radio Button"
        name="radioButtonError"
        transparent="true"
        input-id="choiceOne"
      ></scale-radio-button>
      <scale-radio-button
        status="error"
        value="2"
        label="Radio Button"
        name="radioButtonError"
        input-id="choiceTwo"
      ></scale-radio-button>
    </scale-radio-button-group>
    </div>
  `,
});

## Standard

<Canvas withSource="none">
  <Story
    name="Standard"
    args={{
      label: 'Group Label',
    }}
  >
    {StandardTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html

```

## Helper Text

<Canvas withSource="none">
  <Story
    name="Helper Text"
    args={{
      label: 'Group Label',
      helperText: 'Helper message',
    }}
  >
    {HelperTextTemplate.bind({})}
  </Story>
</Canvas>

```html

```

## Error

<Canvas withSource="none">
  <Story
    name="Error"
    args={{
      label: 'RadioButton Group',
      status: 'error',
      helperText: 'Error message',
    }}
  >
    {ErrorTemplate.bind({})}
  </Story>
</Canvas>

```html

```
