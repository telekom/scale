import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { footerNavigation } from '../brand-header-navigation/fixtures';
import AppFooter from './AppFooter.vue';
const setColorMode = () => {
  try {
    document.body.dataset.mode = JSON.parse(
      window.localStorage.getItem('persistedColorMode')
    );
  } catch (e) {}
};

<Meta
  title="Components/Footer"
  component={AppFooter}
  argTypes={{
    footerNavigation: {
      control: {
        type: 'text',
      },
      description: 'each item takes an optional onClick handler',
    },
    claimLang: {
      control: {
        type: 'select',
        options: ['en', 'de'],
      },
    },
    variant: {
      control: { type: 'select', options: ['angular', 'standard', 'minimal'] },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { AppFooter },
  props: AppFooter.props,
  template: `
    <div style="margin: -1rem;">
      <scale-app-footer
        :claim-lang="claimLang"
        :footer-navigation="footerNavigation"
        :variant="variant"
        :copyright="copyright"
      >
      </scale-app-footer>
    </div>
  `,
});

<div
  style={{
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  }}
>
  <h1>Footer</h1>
  <img src="assets/aa.png" alt="Accessible AA" />
</div>

## Standard

<Canvas withSource="none">
  <Story
    name="Standard"
    inline={false}
    height={500}
    args={{
      claimLang: 'en',
      footerNavigation: JSON.stringify(footerNavigation),
    }}
  >
    {(() => {
      setColorMode();
      return Template.bind({});
    })()}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-app-footer claim-lang="en"></scale-app-footer>
```

### Scoped CSS variables

```css
:host {
  --background: var(--scl-color-black);
  --color: var(--scl-color-white);
  --spacing-top: var(--scl-spacing-32);
  --spacing-bottom: var(--scl-spacing-24);
  --spacing-x: var(--scl-spacing-24);
  --background-container: var(--scl-color-background-standard, #fff);
  --radius: var(--scl-radius-4);
  --max-width: inherit;

  --border-top-minimal: var(--scl-spacing-1) solid var(--scl-color-grey-20);
  --color-minimal: var(--scl-color-grey-60);
  --background-minimal: var(--scl-color-background-standard);

  --spacing-angular: var(--scl-spacing-24);
  --spacing-minimal: var(--scl-spacing-24);

  --background-mask: var(--scl-color-background-standard, #fff);
  --radius-mask: 0 0 var(--scl-radius-8) var(--scl-radius-8);

  --height-logo: var(--scl-spacing-24);
  --font-size-copyright: var(--scl-font-size-14);
  --color-link-standard: var(--scl-color-white);
  --color-link-minimal: var(--scl-color-grey-60);

  --font-size-navigation-standard: var(--scl-font-size-14);
  --font-family-navigation-standard: var(--scl-font-family-sans);
  --font-weight-navigation-standard: var(--scl-font-weight-extrabold);
  --font-weight-navigation-minimal: var(--scl-font-weight-medium);

  --spacing-navigation-standard: var(--scl-spacing-8);
  --color-navigation-standard-hover: var(--scl-color-white);
  --color-navigation-minimal-hover: var(--scl-color-primary-hover);
  --color-navigation-active-focus: var(--scl-color-primary-active);
  --box-shadow-navigation-focus: 0 0 0 var(--scl-spacing-2) var(
      --scl-color-primary
    );

  --border-color-standard-hover: var(--scl-color-white);
}
```

For Shadow Parts, please inspect the element's #shadow.

## Minimal

<Canvas withSource="none">
  <Story
    name="Minimal"
    inline={false}
    height={500}
    args={{
      claimLang: 'en',
      footerNavigation: JSON.stringify(footerNavigation),
      variant: 'minimal',
    }}
  >
    {(() => {
      setColorMode();
      return Template.bind({});
    })()}
  </Story>
</Canvas>

```html
<scale-app-footer id="footer" claim-lang="en" variant="minimal">
</scale-app-footer>

<script>
  const footer = document.getElementById('footer');
  footer.footerNavigation = [
    { name: 'Contact', id: 'Contact', href: '#contact' },
    {
      name: 'Terms and conditions',
      id: 'Terms and conditions',
      href: '#terms-and-conditions',
    },
    { name: 'Legal notice', id: 'Legal notice', href: '#legal-notice' },
    {
      name: 'Data privacy',
      id: 'Data privacy',
      href: '#data-privacy',
      icon: 'alert-imprint-dataprivacy',
    },
  ];
</script>
```

## Custom Slots

<Canvas withSource="none">
  <Story
    name="Custom Slots"
    inline={false}
    height={500}
    args={{
      claimLang: 'en',
    }}
  >
    {() => (
      setColorMode(),
      {
        components: { AppFooter },
        props: AppFooter.props,
        template: `
        <div style="margin: -1rem;">
          <scale-app-footer
            :claim-lang="claimLang"
            :variant="variant"
            :copyright="copyright"
          >
            <div slot="navigation">
              <span>custom navigation placeholder</span>
            </div>
            <div slot="logo">
              <span>custom logo placeholder</span>
            </div>
          </scale-app-footer>
        </div>
      `,
      }
    )}
  </Story>
</Canvas>

```html
<scale-app-footer
  :claim-lang="claimLang"
  :variant="variant"
  :copyright="copyright"
>
  <div slot="navigation">
    <span>custom navigation placeholder</span>
  </div>
  <div slot="logo">
    <span>custom logo placeholder</span>
  </div>
</scale-app-footer>
```
