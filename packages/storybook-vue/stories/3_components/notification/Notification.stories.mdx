import {
  Meta,
  Story,
  ArgsTable,
  Canvas,
  Description,
} from '@storybook/addon-docs/blocks';
import ScaleNotification from './ScaleNotification.vue';
import ScaleNotificationToast from './ScaleNotificationToastProps.vue';
import ScaleNotificationBanner from './ScaleNotificationBannerProps.vue';
import ScaleNotificationInline from './ScaleNotificationInlineProps.vue';

<Meta
  title="Beta Components/Notification"
  component={ScaleNotification}
  subcomponents={{
    'Toast Notification': ScaleNotificationToast,
    'Banner Notification': ScaleNotificationBanner,
    'Inline Notification': ScaleNotificationInline,
  }}
  argTypes={{
    type: {
      defaultValue: 'inline',
      options: ['banner', 'inline', 'toast'],
      control: {
        type: 'select',
      },
    },
    variant: {
      defaultValue: 'informational',
      options: ['error', 'warning', 'success', 'informational'],
      control: {
        type: 'select',
      },
    },
    opened: {
      control: {
        type: 'boolean',
      },
    },
    dismissible: {
      defaultValue: false,
      control: {
        type: 'boolean',
      },
    },
    autoHide: {
      defaultValue: false,
      control: {
        type: 'boolean',
      },
    },
    autoHideDuration: {
      defaultValue: 3000,
      control: {
        type: 'number',
      },
    },
    href: {},
    styles: {},
    toastAnimated: {
      defaultValue: true,
      control: {
        type: 'boolean',
      },
    },
    toastAlignment: {
      defaultValue: 'top-right',
      options: ['bottom-right', 'bottom-left', 'top-right', 'top-left'],
      control: {
        type: 'select',
      },
    },
    toastPositionVertical: {
      defaultValue: 12,
      control: {
        type: 'number',
      },
    },
    toastPositionHorizontal: {
      defaultValue: 12,
      control: {
        type: 'number',
      },
    },
    toastFadeDuration: {
      defaultValue: 500,
      control: {
        type: 'number',
      },
    },
    toastStory: { control: { type: '' } },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: {
    ...ScaleNotification.props,
    heading: String,
    text: String,
    link: String,
  },
  template: `
  <div style="height: 100px">
    <scale-notification  
      :type="type" 
      :variant="variant"
      :dismissible="dismissible"
      :auto-hide-duration="autoHideDuration"
      :auto-hide="autoHide"
      :opened="opened"
      :href="href"
      :styles="styles"
      :toast-animated="toastAnimated"
      :toast-alignment="toastAlignment"
      :toast-position-vertical="toastPositionHorizontal"
      :toast-position-horizontal="toastPositionVertical"
      :toast-fade-duration="toastFadeDuration"
      :toast-story="toastStory"
    >
      {{ heading }} 
      <p v-if="text">{{ text }}</p>
      <p v-if="link">{{ link }}</p>
    </scale-notification>
    </div>
  `,
});

export const ToastTemplate = (args, { argTypes }) => ({
  props: {
    ...ScaleNotification.props,
  },
  template: `
  <div style="height: 100px">
    <scale-notification
      :type="type" 
      :variant="variant"
      :dismissible="dismissible"
      :auto-hide-duration="autoHideDuration"
      :auto-hide="autoHide"
      :opened="opened"
      :href="href"
      :styles="styles"
      :toast-animated="toastAnimated"
      :toast-alignment="toastAlignment"
      :toast-position-vertical="toastPositionHorizontal"
      :toast-position-horizontal="toastPositionVertical"
      :toast-fade-duration="toastFadeDuration"
      :toast-story="toastStory"
      >
       <p>Hello World</p>
    </scale-notification>
    </div>`,
});

<div
  style={{
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  }}
>
  <h1>Notification</h1>
  <img src="assets/beta.png" alt="Beta Component" />
</div>

## Standard

<Canvas withSource="none">
  <Story
    components={ScaleNotification}
    name="Standard"
    args={{
      opened: true,
      heading: 'Notification text goes here',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

## Toast

<Canvas withSource="none">
  <Story
    components={ScaleNotification}
    name="Toast"
    args={{
      opened: true,
      type: 'toast',
      toastStory: true,
    }}
  >
    {ToastTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-notification opened>Notification text goes here</scale-notification>
```
