import {
  Meta,
  Story,
  ArgsTable,
  Canvas,
  Description,
} from '@storybook/addon-docs/blocks';
import ScaleTooltip from './ScaleTooltip.vue';

<Meta
  title="Beta Components/Tooltip"
  component={ScaleTooltip}
  argTypes={{
    content: {
      control: { type: 'text' },
    },
    placement: {
      control: {
        type: 'select',
        options: [
          'top',
          'top-start',
          'top-end',
          'right',
          'right-start',
          'right-end',
          'bottom',
          'bottom-start',
          'bottom-end',
          'left',
          'left-start',
          'left-end',
        ],
      },
    },
    disabled: {
      defaultValue: false,
      control: { type: 'boolean' },
    },
    distance: {
      defaultValue: 5,
      control: { type: 'text' },
    },
    open: {
      defaultValue: false,
      control: { type: 'boolean' },
    },
    skidding: {
      defaultValue: false,
      control: { type: 'boolean' },
    },
    trigger: {
      control: { type: 'text' },
    },
    flip: {
      defaultValue: false,
      control: { type: 'boolean' },
    },
    preventOverflow: {
      defaultValue: false,
      control: { type: 'boolean' },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleTooltip },
  props: {
    ...ScaleTooltip.props,
  },
  template: `
    <scale-tooltip
      :content="content"
      :placement="placement"
      :disabled="disabled"
      :distance="distance"
      :open="open"
      :skidding="skidding"
      :trigger="trigger"
      :flip="flip"
      :preventOverflow="preventOverflow"
    >
    <scale-button>Hallo</scale-button>
    </scale-tooltip>
  `,
});

export const SlotTemplate = (args, { argTypes }) => ({
  components: { ScaleTooltip },
  props: {
    ...ScaleTooltip.props,
  },
  template: `
    <scale-tooltip
      :content="content"
      :placement="placement"
      :disabled="disabled"
      :distance="distance"
      :open="open"
      :skidding="skidding"
      :trigger="trigger"
      :flip="flip"
      :preventOverflow="preventOverflow"
    >
    <template v-slot:content>
        <a slot="content" href="https://github.com/telekom/scale" style="color: white;background-color: transparent;">Go to Scale</a>
    </template>
    <scale-button>Hallo</scale-button>
    </scale-tooltip>
  `,
});

## Standard

<Canvas withSource="none">
  <Story
    name="Standard"
    args={{
      content: 'Tooltip',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-tooltip content="Tooltip">
  <scale-button>Hover to show Tooltip</scale-button>
</scale-tooltip>
```

## Placement

### Left

<Canvas withSource="none">
  <Story
    name="Placement-Left"
    args={{
      content: 'Tooltip',
      placement: 'left',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-tooltip content="Tooltip" placement="left">
  <scale-button>Hover to show Tooltip</scale-button>
</scale-tooltip>
```

### Left-Start

<Canvas withSource="none">
  <Story
    name="Placement-Left-Start"
    args={{
      content: 'Tooltip',
      placement: 'left-start',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-tooltip content="Tooltip" placement="left-start">
  <scale-button>Hover to show Tooltip</scale-button>
</scale-tooltip>
```

### Left-End

<Canvas withSource="none">
  <Story
    name="Placement-Left-End"
    args={{
      content: 'Tooltip',
      placement: 'left-end',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-tooltip content="Tooltip" placement="left-end">
  <scale-button>Hover to show Tooltip</scale-button>
</scale-tooltip>
```

## Disabled

<Canvas withSource="none">
  <Story
    name="Disabled"
    args={{
      content: 'Tooltip',
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-tooltip content="Tooltip" disabled="true">
  <scale-button>Hover to show Tooltip</scale-button>
</scale-tooltip>
```

## Slot

<Canvas withSource="none">
  <Story name="Slot" args={{}}>
    {SlotTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-tooltip content="Tooltip">
  <a slot="content" href="https://github.com/telekom/scale">Go to Scale</a>
  <scale-button>Hover to show Tooltip</scale-button>
</scale-tooltip>
```
