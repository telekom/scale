import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import ScaleLoadingSpinner from './ScaleLoadingSpinner.vue';
import { action } from '@storybook/addon-actions';

<Meta
  title="Beta Components/Loading-Spinner"
  component={ScaleLoadingSpinner}
  argTypes={{}}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleLoadingSpinner },
  props: {
    label: String,
    ...ScaleLoadingSpinner.props,
  },
  template: `
    <scale-loading-spinner
       :variant='variant'
    :alignment='alignment'
    :text='text'
    :size='size'
    >
    </scale-loading-spinner>
    `,
});
export const LargeBlackTemplate = (args, { argTypes }) => ({
  components: { ScaleLoadingSpinner },
  props: {
    label: String,
    ...ScaleLoadingSpinner.props,
  },
  template: `
  <div style="background-color: black; height:52px; padding:5px">
    <scale-loading-spinner
       :variant='variant'
    :alignment='alignment'
    :text='text'
    :size='size'
    >
    </scale-loading-spinner>
    </div>
    `,
});

export const LargeVerticalBlackTemplate = (args, { argTypes }) => ({
  components: { ScaleLoadingSpinner },
  props: {
    label: String,
    ...ScaleLoadingSpinner.props,
  },
  template: `
  <div style="background-color: black; height:70px; padding:5px">
    <scale-loading-spinner
       :variant='variant'
    :alignment='alignment'
    :text='text'
    :size='size'
    >
    </scale-loading-spinner>
    </div>
    `,
});

export const SmallBlackTemplate = (args, { argTypes }) => ({
  components: { ScaleLoadingSpinner },
  props: {
    label: String,
    ...ScaleLoadingSpinner.props,
  },
  template: `
  <div style="background-color: black; padding:5px">
    <scale-loading-spinner
       :variant='variant'
    :alignment='alignment'
    :text='text'
    :size='size'
    >
    </scale-loading-spinner>
    </div>
    `,
});

# Loading-Spinner

## Standard

<Canvas withSource="none">
  <Story name="Standard" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

## Small Size & Horizontal Text (Primary)

<Canvas withSource="none">
  <Story
    name="Small Primary (horizontal)"
    args={{
      text: 'Loading...',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-loading-spinner text="Loading..."></scale-loading-spinner>
```

## Small Size & Vertical Text (Primary)

<Canvas withSource="none">
  <Story
    name="Small Primary (vertical)"
    args={{
      text: 'Loading...',
      alignment: 'vertical',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-loading-spinner
  text="Loading..."
  alignment="vertical"
></scale-loading-spinner>
```

## Large Size & Horizontal Text (Primary)

<Canvas withSource="none">
  <Story
    name="Large Primary (horizontal)"
    args={{
      size: 'large',
      text: 'Loading...',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-loading-spinner size="large" text="Loading..."></scale-loading-spinner>
```

## Large Size & Vertical Text (Primary)

<Canvas withSource="none">
  <Story
    name="Large Primary (vertical)"
    args={{
      size: 'large',
      text: 'Loading...',
      alignment: 'vertical',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-loading-spinner
  size="large"
  text="Loading..."
  alignment="vertical"
></scale-loading-spinner>
```

## Small Size (White)

<Canvas withSource="none">
  <Story
    name="Small White"
    args={{
      size: 'small',
      variant: 'white',
    }}
  >
    {SmallBlackTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-loading-spinner size="small" variant="white"></scale-loading-spinner>
```

## Small Size & Horizontal Text (White)

<Canvas withSource="none">
  <Story
    name="Small White (horizontal)"
    args={{
      size: 'small',
      text: 'Loading...',
      variant: 'white',
    }}
  >
    {SmallBlackTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-loading-spinner
  size="small"
  text="Loading..."
  variant="white"
></scale-loading-spinner>
```

## Small Size & Vertical Text (White)

<Canvas withSource="none">
  <Story
    name="Small White (vertical)"
    args={{
      size: 'small',
      text: 'Loading...',
      variant: 'white',
      alignment: 'vertical',
    }}
  >
    {SmallBlackTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-loading-spinner
  size="small"
  text="Loading..."
  variant="white"
  alignment="vertical"
></scale-loading-spinner>
```

## Large Size (White)

<Canvas withSource="none">
  <Story
    name="Large White"
    args={{
      size: 'large',
      variant: 'white',
    }}
  >
    {LargeBlackTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-loading-spinner size="large" variant="white"></scale-loading-spinner>
```

## Large Size & Horizontal Text (White)

<Canvas withSource="none">
  <Story
    name="Large White (horizontal)"
    args={{
      size: 'large',
      text: 'Loading...',
      variant: 'white',
    }}
  >
    {LargeBlackTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-loading-spinner
  size="large"
  text="Loading..."
  variant="white"
></scale-loading-spinner>
```

## Large Size & Vertical Text (White)

<Canvas withSource="none">
  <Story
    name="Large White (vertical)"
    args={{
      size: 'large',
      variant: 'white',
      alignment: 'vertical',
      text: 'Loading...',
    }}
  >
    {LargeVerticalBlackTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-loading-spinner
  size="large"
  variant="white"
  alignment="vertical"
  text="Loading..."
></scale-loading-spinner>
```
