import { Meta, Story, ArgsTable, Canvas, Description } from "@storybook/addon-docs/blocks";
import ScaleCheckbox from "./ScaleCheckbox.vue";
import { action } from "@storybook/addon-actions";

<Meta
  title="Components/Checkbox"
  component={ScaleCheckbox}
  argTypes={{}}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleCheckbox },
  props: {
    label: String,
    ...ScaleCheckbox.props
  },
  template: `
    <scale-checkbox
      :name="name"
      :label="label"
      :helper-text="helperText"
      :disabled="disabled"
      :checked="checked"
      :value="value"
      :status="status"
      :input-id="inputId"
      @scaleChange="scaleChange"
    ></scale-checkbox>
  `,
  methods: {
    scaleChange: function() {
      action('scaleChange')();
    },
  },
})

<div style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'space-between', width: '100%'}}>
    <h1>Checkbox</h1>
    <img src="assets/aa.png" alt="Accessible AA" />
</div>


## Standard

<Canvas withSource="none">
  <Story
    name="Standard"
    args={{
      label: "Checkbox",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

### Scoped CSS variables

```css
scale-checkbox {
  --spacing-x: var(--scl-spacing-8);
  --transition: all var(--scl-motion-duration-fast) var(--scl-motion-easing-standard);
  --color-text: var(--scl-color-text-standard);
  --color-error: var(--scl-color-text-error);
  --color-disabled: var(--scl-color-background-disabled);
  --color-standard: var(--scl-color-background-standard);
  --background-disabled: var(--scl-color-white);
  --color-primary: var(--scl-color-primary, #e20074);
  --color-focus: var(--scl-color-focus);
  --color-primary-hover: var(--scl-color-primary-hover, #f90984);
  --color-primary-active: var(--scl-color-primary-active, #cb0068);

  /*control*/
  --width-control: var(--scl-spacing-16);
  --height-control: var(--scl-spacing-16);
  --transition-control: var(--transition);
  --spacing-control: var(--scl-spacing-2);
  --spacing-left-control: var(--scl-spacing-4);
  --radius-control: var(--scl-radius-4);
  --border-width-control: var(--scl-spacing-1);

  /*helper-text*/
  --transition-helper-text: var(--transition);
  --spacing-left-helper-text: calc(var(--width-control) + var(--spacing-x));
  --font-size-helper-text: var(--scl-font-size-12);
  --font-weight-helper-text: var(--scl-font-weight-bold);
  --line-height-helper-text: var(--scl-font-line-height-133);
  --color-helper-text: var(--scl-color-blue-70);

  /*label*/
  --font-weight-label: var(--scl-font-weight-medium);
  --transition-label: var(--transition);

  /*icon*/
  --color-icon-checked-disabled: var(--scl-color-grey-30);
  --color-icon-checked-active: var(--scl-color-white);
  --width-icon: var(--scl-spacing-12);
  --height-icon: var(--scl-spacing-12);
  --left-icon: 50%;
  --top-icon: 50%;
  --transform-icon: translate(-50%, -50%);
}
```

## Standard Disabled

<Canvas withSource="none">
  <Story
    name="Standard Disabled"
    args={{
      label: "Standard Disabled",
      disabled: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-checkbox disabled label="Standard Disabled"></scale-checkbox>
```

## Selected

<Canvas withSource="none">
  <Story
    name="Selected"
    args={{
      label: "Selected",
      checked: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-checkbox checked label="Selected"></scale-checkbox>
```

## Selected Disabled

<Canvas withSource="none">
  <Story
    name="Selected Disabled"
    args={{
      label: "Selected Disabled",
      checked: true,
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-checkbox checked disabled label="Selected Disabled"></scale-checkbox>
```

## Helper Text

<Canvas withSource="none">
    <Story
        name="Helper Text"
        args={{
            label: "Helper Text",
            helperText: "Make sure to check this"
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

```html
<scale-checkbox label="Helper Text" helper-text="Make sure to check this"></scale-checkbox>
```


## Error

<Canvas withSource="none">
  <Story
    name="Error"
    args={{
      label: "Standard with error",
      status: "error",
      helperText: "This is the error message"
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-checkbox status="error" label="Standard with error"></scale-checkbox>
```


## Custom Label Slot

<Canvas withSource="none">
  <Story
    name="Custom Label Slot"
    args={{}}
  >
    {() => ({
        components: { ScaleCheckbox },
        props: {
            label: String,
            ...ScaleCheckbox.props
        },
        template: `<scale-checkbox><span style="cursor: pointer; display: flex; align-items: center;">Custom label &nbsp;<scale-icon-alert-imprint-dataprivacy></scale-icon-alert-imprint-dataprivacy></span></scale-checkbox>`,
    })}
  </Story>
</Canvas>

```html
<scale-checkbox><span style="cursor: pointer; display: flex; align-items: center;">Custom label &nbsp;<scale-icon-alert-imprint-dataprivacy></scale-icon-alert-imprint-dataprivacy></span></scale-checkbox>
```
