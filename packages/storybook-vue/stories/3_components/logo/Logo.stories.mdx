import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import ScaleLogo from './ScaleLogo.vue';
import { action } from '@storybook/addon-actions';

<Meta
  title="Beta Components/Logo"
  component={ScaleLogo}
  argTypes={{
    language: {
      control: {
        type: 'select',
        options: [
          'de',
          'en',
          'cz',
          'hr',
          'hu',
          'me',
          'mk_kyr',
          'mk_lat',
          'ro',
          'sk',
          '',
        ],
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleLogo },
  props: {
    label: String,
    ...ScaleLogo.props,
  },
  template: `
    <scale-logo
        :variant="variant"
        :transparent="transparent"
        :language="language"
        :size="size"
        :href="href"
        :accessibility-title="accessibilityTitle"
        @click-handler="clickHandler"
    >
    </scale-logo>
    `,
  methods: {
    clickHandler: action('clickHandler'),
  },
});

# Logo

## Standard

<Canvas withSource="none">
  <Story name="Standard" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

## White

<Canvas withSource="none">
  <Story
    name="White"
    args={{
      variant: 'white',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-logo variant="white"></scale-logo>
```

## Sizing

<Canvas withSource="none">
  <Story
    name="Sizing"
    args={{
      size: 100,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-logo size="100"></scale-logo>
```

## Link

<Canvas withSource="none">
  <Story
    name="Link"
    args={{
      href: 'https://www.telekom.de/start',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-logo href="https://www.telekom.de/start"></scale-logo>
```

## Macedonia (MK) - Cyrillic

<Canvas withSource="none">
  <Story
    name="Macedonia (MK) - Cyrillic"
    args={{
      language: 'mk_kyr',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-logo language="mk_kyr"></scale-logo>
```

## Macedonia (MK) - Latin script

<Canvas withSource="none">
  <Story
    name="Macedonia (MK) - Latin script"
    args={{
      language: 'mk_lat',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-logo language="mk_lat"></scale-logo>
```
