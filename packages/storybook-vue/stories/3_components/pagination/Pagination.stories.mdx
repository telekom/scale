import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import ScalePagination from './ScalePagination.vue';

<Meta
  title="Beta Components/Pagination"
  component={ScalePagination}
  argTypes={{
    hideBorders: {
      table: {
        type: { summary: 'Boolean' },
        defaultValue: { summary: false },
      },
      description: `(optional) Set to true to hide top and bottom borders`,
      control: { type: null },
    },
    pageSize: {
      table: {
        type: { summary: 'number' },
        defaultValue: { summary: 10 },
      },
      description: `(optional) Set number of rows/elements to show per page`,
      control: { type: null },
    },
    startElement: {
      table: {
        type: { summary: 'number' },
        defaultValue: { summary: 0 },
      },
      description: `(optional) Index of first element to display`,
      control: { type: null },
    },
    totalElements: {
      table: {
        type: { summary: 'number' },
        defaultValue: { summary: 1 },
      },
      description: `(optional) Total number of rows/elements used to calculate page displays`,
      control: { type: null },
    },
    styles: {
      table: {
        type: { summary: 'string' },
      },
      description: `(optional) Injected styles`,
      control: { type: null },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScalePagination },
  props: {
    ...ScalePagination.props,
  },
  template: `
    <scale-pagination
    :hide-borders="hideBorders"
    :page-size="pageSize"
    :small="small"
    :start-element="startElement"
    :total-elements="totalElements"
    :lang-go-first="langGoFirst"
    :lang-go-last="langGoLast"
    :lang-go-previous="langGoPrevious"
    :lang-go-next="langGoNext"
    :styles="styles"
  >
  </scale-pagination>
        `,
});

export const TemplateEmbedded = (args, { argTypes }) => ({
  components: { ScalePagination },
  props: {
    ...ScalePagination.props,
  },
  template: `
  <div style="border: 1px solid #000; display: flex; align-items: center;  overflow: auto;">
    <scale-pagination
        :hide-borders="hideBorders"
        :page-size="pageSize"
        :small="small"
        :start-element="startElement"
        :total-elements="totalElements"
        :styles="styles"
    >
    </scale-pagination>
  </div>`,
});

<div
  style={{
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  }}
>
  <h1>Pagination</h1>
  <img src="assets/beta.png" alt="Beta Component" />
</div>

### Properties

## Standard

<Canvas withSource="none">
  <Story 
    name="Standard"
    args={{
        pageSize: 10,
        startElement: 100,
        totalElements: 200
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-pagination page-size="10" start-element="100" total-elements="200"></scale-pagination>
```

## Small

<Canvas withSource="none">
  <Story 
    name="Small"
    args={{
        small: true,
        pageSize: 5,
        startElement: 100,
        totalElements: 200
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-pagination page-size="5" start-element="100" total-elements="200" small></scale-pagination>
```

## Hidden Borders

<Canvas withSource="none">
  <Story 
    name="Hidden Borders"
    args={{
        pageSize: 5,
        startElement: 100,
        totalElements: 200,
        hideBorders: true
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-pagination page-size="5" start-element="100" total-elements="200" hide-borders></scale-pagination>
```



## Embedded Hidden Borders

<Canvas withSource="none">
  <Story 
    name=" Embedded Hidden Borders"
    args={{
        pageSize: 5,
        startElement: 100,
        totalElements: 200,
        hideBorders: true
    }}>
    
        {TemplateEmbedded.bind({})}
  </Story>
</Canvas>

```html
<div style="border: 1px solid #000; display: flex; align-items: center;  overflow: auto;">
 <scale-pagination page-size="5" start-element="100" total-elements="200" hide-borders></scale-pagination>
</div>
```

## Translation German

<Canvas withSource="none">
  <Story 
    name="Translation German"
    args={{
        pageSize: 10,
        startElement: 100,
        totalElements: 200,
        langGoFirst: 'Zur ersten Seite',
        langGoLast: 'Zur letzten Seite',
        langGoPrevious: 'Zur vorigen Seite',
        langGoNext: 'Zur nächsten Seite',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-pagination 
    page-size="10" 
    start-element="100" 
    total-elements="200"
    lang-go-first="Zur ersten Seite"
    lang-go-next="Zur nächsten Seite"
    lang-go-previous="Zur vorigen Seite"
    lang-go-last="Zur letzten Seite"
    ></scale-pagination>
```
