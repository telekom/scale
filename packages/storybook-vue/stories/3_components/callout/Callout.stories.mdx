import {
  Meta,
  Story,
  ArgsTable,
  Canvas,
  Description,
} from '@storybook/addon-docs/blocks';
import ScaleCallout from './ScaleCallout.vue';

<Meta
  title="Beta Components/Callout"
  component={ScaleCallout}
  argTypes={{
    variant: {
      defaultValue: 'primary',
      options: ['primary', 'white', 'black', 'blue'],
      control: {
        type: 'select',
      },
    },
    size: {
      defaultValue: 'large',
      options: ['small', 'large'],
      control: {
        type: 'select',
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleCallout },
  props: ScaleCallout.props,
  template: `
      <scale-callout :size="size" :variant="variant" :rotation="rotation">
      <div>
        <div style="text-align:center; font-size:20px; color:white; font-weight:400;">
          Für nur 
        </div>
        <div style="text-align:center; font-size: 60px; color: white; font-weight: 700;">
          <span>
            15 €
          </span>
        </div>
      </div>
    </scale-callout>
  `,
});

export const Template2 = (args, { argTypes }) => ({
  components: { ScaleCallout },
  props: ScaleCallout.props,
  template: `
      <scale-callout :size="size" :variant="variant" :rotation="rotation">
      <div>
        <div style="text-align: center; font-size: 15px; color: white; font-weight: 400; margin-bottom: 10px;">
          Für nur
        </div>
        <div style="text-align: center; font-size: 40px; color: white; font-weight: 700;">
          <span>
            15 €
          </span>
        </div>
      </div>
    </scale-callout>
  `,
});

export const Template3 = (args, { argTypes }) => ({
  components: { ScaleCallout },
  props: ScaleCallout.props,
  template: `
      <scale-callout :size="size" :variant="variant" :rotation="rotation">
      <div>
        <div style="text-align: center; font-size: 25px; color: white; font-weight: 400; margin-bottom: 10px;">
          ab
        </div>
        <div style="text-align: center; font-size: 35px; color: white; font-weight: 700;">
          <span>
            19,95 €
          </span>
          <sup title="This offer is only valid for a limited time. Free shipping on all orders." style="font-size: 30px; color: white; cursor: help;">*</sup>
        </div>
      </div>
    </scale-callout>
  `,
});

## Standard
### All default values
<Canvas withSource="none">
  <Story
    name="Standard"
    args={{
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-callout>
  <div>
    <div
      style="text-align:center; font-size:20px; color:white; font-weight:400;"
    >
      Für nur
    </div>
    <div
      style="text-align:center; font-size: 60px; color: white; font-weight: 700;"
    >
      <span> 15 € </span>
    </div>
  </div>
</scale-callout>
```

### Scoped CSS variables
```css
 :host {
  --color-callout: var(--scl-color-primary);
  --inner-width-small: var(--scl-spacing-80);
  --inner-height-small: var(--scl-spacing-80);
  --inner-width-large: 126.5px;
  --inner-height-large: 96px;
  --width-small: 120px;
  --height-small: 120px;
  --height-large: 160px;
  --width-large: 160px;
  --blue-callout: var(--scl-color-blue-60);
  --white-callout: var(--scl-color-white);
  --black-callout: var(--scl-color-black);
  --primary-callout: var(--scl-color-primary);
}
}
```
## Size
There are two sizes: 
Large / small. Large has the size 160x160 and small 120x120. In the example below, the callout is small

<Canvas withSource="none">
  <Story
    name="Sizes"
    args={{
      size: "small",
    }}
  >
    {Template2.bind({})}
  </Story>
</Canvas>

```html
<scale-callout size="small" >
  <div>
    <div
      style="text-align:center; font-size:20px; color:white; font-weight:400;"
    >
      Für nur
    </div>
    <div
      style="text-align:center; font-size: 60px; color: white; font-weight: 700;"
    >
      <span> 15 € </span>
    </div>
  </div>
</scale-callout>
```

## Rotation
<Canvas withSource="none">
  <Story
    name="Rotation"
    args={{
      size: "small",
      variant: "blue",
      rotation: 33,
    }}
  >
    {Template2.bind({})}
  </Story>
</Canvas>

```html
<scale-callout variant="blue" size="small" rotation="33" >
  <div>
    <div
      style="text-align:center; font-size:20px; color:white; font-weight:400;"
    >
      Für nur
    </div>
    <div
      style="text-align:center; font-size: 60px; color: white; font-weight: 700;"
    >
      <span> 15 € </span>
    </div>
  </div>
</scale-callout>
```

## Variants
There are 4 standard colors (Primary - default, black, white, blue). The user also has the possibilty to determine another color by accessing the specific css class. 

<Canvas withSource="none">
  <Story
    name="Variants"
    args={{
      size: "small",
      variant: "black"
    }}
  >
    {Template2.bind({})}
  </Story>
</Canvas>

```html
<scale-callout size="small" variant="black">
      <div>
        <div style="text-align:center; font-size:20px; color:white; font-weight:400;">
          Für nur
        </div>
        <div style="text-align:center; font-size: 60px; color: white; font-weight: 700;">
          <span>
            15 €
          </span>
        </div>
      </div>
</scale-callout>
```

## Asterisk
<Canvas withSource="none">
  <Story
    name="Asterisk"
    args={{
    }}
  >
    {Template3.bind({})}
  </Story>
</Canvas>

```html
<scale-callout>
      <div>
        <div style="text-align: center; font-size: 25px; TeleNeo; color: white; font-weight: 400; margin-bottom: 10px;">
          ab
        </div>
        <div style="text-align: center; font-size: 35px; TeleNeo; color: white; font-weight: 700;">
          <span>
            19,95 €
          </span>
          <sup title="This offer is only valid for a limited time. Free shipping on all orders." style="font-size: 30px; color: white; cursor: help;">*</sup>
        </div>
      </div>
</scale-callout>
```