
import {
  Meta,
  Story,
  ArgsTable,
  Canvas,
  Description,
} from '@storybook/addon-docs/blocks';
import ScaleAlertbox from './ScaleAlertbox.vue';

<Meta
  title="Beta Components/Alertbox"
  component={ScaleAlertbox}
  argTypes={{
    variant: {
      defaultValue: 'informational',
      options: ['error', 'warning', 'success', 'informational'],
      control: {
        type: 'select',
      },
    },
    hasClose: {
      defaultValue: false,
      options: [true, false],
      control: {
        type: 'select',
      },
    },
    opened: {
      options: [true, false],
      control: {
        type: 'select',
      },
    },
    timout: {
      defaultValue: false,
      options: [false, true, Number],
      control: {
        type: 'select',
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleAlertbox },
  props: {
    ...ScaleAlertbox.props,
  },
  template: `
    <scale-alertbox   
    :variant="variant"
    :hasClose="hasClose"
    :timeout="timeout"
    :opened="opened"
    >
    <p slot="header">testing</p>
    <p slot="text">testing</p>
    </scale-alertbox>
  `
});

## Standard

### All default values

<Canvas withSource="none">
  <Story name="Standard" args={{
    opened: true,
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-alertbox variant="informational" opened>
</scale-alertbox>
```


## Scoped CSS variables

```css
:host {
  --corner-radius: 8px;
  --variant-error: hsla(353.79999999999995, 100%, 88.6%, .35);
  --variant-warning: hsla(33.5, 100%, 75.1%, .3);
  --variant-informational: hsla(190.20000000000005, 100%, 92%, .75);
  --variant-success: hsl(98.80000000000001, 49.7%, 61%, .2);
}
```

## Variants
### Variants available :: error, warning, success, informational
<Canvas withSource="none">
  <Story name="Variants" args={{
    opened: true,
    variant: "warning"
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-alertbox variant="warning" opened>
</scale-alertbox>
```


## Close Button
<Canvas withSource="none">
  <Story name="Close Button" args={{
    opened: true,
    variant: "error",
    hasClose: true
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-alertbox variant="error" has-close opened>
</scale-alertbox>
```


## Timeout
### Set to close in 10 seconds. Default - when true: is 3 seconds
<Canvas withSource="none">
  <Story name="Timeout" args={{
    opened: true,
    variant: "success",
    hasClose: true,
    timeout: 10000
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-alertbox variant="success" timeout=10000 has-close opened>
</scale-alertbox>
```


## Titel & Message
<Canvas withSource="none">
  <Story name="Titel-Message" args={{
    opened: true,
    variant: "warning",
    hasClose: true,
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-alertbox variant="warning" timeout has-close opened>
  <p slot="header">This is my title</p>
  <p slot="header">This is my message to you.</p>
</scale-alertbox>
```