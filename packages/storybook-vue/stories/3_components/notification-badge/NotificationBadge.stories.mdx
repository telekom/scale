import {
  Meta,
  Story,
  ArgsTable,
  Canvas,
  Description,
} from '@storybook/addon-docs/blocks';
import ScaleNotificationBadge from './ScaleNotificationBadge.vue';
import { action } from '@storybook/addon-actions';

<Meta
  title="Beta Components/Notification Badge"
  component={ScaleNotificationBadge}
  argTypes={{}}
/>

export const IconTemplate = (args, { argTypes }) => ({
  props: {
    label: String,
    ...ScaleNotificationBadge.props,
  },
  template: `
    <scale-notification-badge
      :type="type"
      :label="label"
    ><scale-icon-user-file-user slot="before-badge"></scale-icon-user-file-user></scale-notification-badge>
  `,
  methods: {
    action,
  },
});

export const TextTemplate = (args, { argTypes }) => ({
  props: {
    label: String,
    ...ScaleNotificationBadge.props,
  },
  template: `
    <scale-notification-badge
      :type="type"
      :label="label"
    ><b slot="before-badge">Service</b></scale-notification-badge>
  `,
  methods: {
    action,
  },
});

## Standard

<Canvas withSource="none">
  <Story name="Standard" args={{}}>
    {IconTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-notification-badge>
  <scale-icon-user-file-user slot="before-badge"> </scale-icon-user-file-user>
</scale-notification-badge>
```

## Badge without Label

### Icon

<Canvas withSource="none">
  <Story
    name="Icon"
    args={{
      type: 'icon',
    }}
  >
    {IconTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-badge type="icon">
  <scale-icon-user-file-user slot="before-badge"></scale-icon-user-file-user>
</scale-notification-badge>
```

### Text

<Canvas withSource="none">
  <Story
    name="Text"
    args={{
      type: 'text',
    }}
  >
    {TextTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-badge type="text">
  <b slot="before-badge">Service</b>
</scale-notification-badge>
```

## Badge with Label

### Icon

<Canvas withSource="none">
  <Story
    name="Label icon"
    args={{
      type: 'icon',
      label: '1000000',
    }}
  >
    {IconTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-badge label="1000000" type="icon">
  <scale-icon-user-file-user slot="before-badge"></scale-icon-user-file-user>
</scale-notification-badge>
```

### Text

<Canvas withSource="none">
  <Story
    name="Label text"
    args={{
      type: 'text',
      label: '1000',
    }}
  >
    {TextTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-badge label="10000" type="text"
  ><b slot="before-badge">Service</b></scale-notification-badge
>
```
