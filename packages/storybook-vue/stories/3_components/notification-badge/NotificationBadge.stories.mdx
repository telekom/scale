import {
  Meta,
  Story,
  ArgsTable,
  Canvas,
  Description,
} from '@storybook/addon-docs/blocks';
import ScaleNotificationBadge from './ScaleNotificationBadge.vue';
import { action } from '@storybook/addon-actions';

<Meta
  title="Components/NotificationBadge"
  component={ScaleNotificationBadge}
  argTypes={{}}
/>

export const IconTemplate = (args, { argTypes }) => ({
  props: {
    label: String,
    ...ScaleNotificationBadge.props,
  },
  template: `
    <scale-notification-badge
      :type="type"
      :label="label"
    ><scale-icon-user-file-user></scale-icon-user-file-user></scale-notification-badge>
  `,
  methods: {
    action,
  },
});

export const TextTemplate = (args, { argTypes }) => ({
  props: {
    label: String,
    ...ScaleNotificationBadge.props,
  },
  template: `
    <scale-notification-badge
      :type="type"
      :label="label"
    >Service</scale-notification-badge>
  `,
  methods: {
    action,
  },
});

## Standard

<Canvas withSource="none">
  <Story name="Standard" args={{}}>
    {IconTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-notification-badge>
  <scale-icon-user-file-user> </scale-icon-user-file-user>
</scale-notification-badge>
```

## Badge without Label

### Icon

<Canvas withSource="none">
  <Story
    name="Icon"
    args={{
      type: 'icon',
    }}
  >
    {IconTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-badge type="icon">
  <scale-icon-user-file-user></scale-icon-user-file-user>
</scale-notification-badge>
```

### Text

<Canvas withSource="none">
  <Story
    name="Text"
    args={{
      type: 'text',
    }}
  >
    {TextTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-badge type="text"> Service </scale-notification-badge>
```

## Badge with Label

### Icon

<Canvas withSource="none">
  <Story
    name="Label_Icon"
    args={{
      type: 'icon',
      label: '999+',
    }}
  >
    {IconTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-badge label="999+" type="icon">
  <scale-icon-user-file-user></scale-icon-user-file-user>
</scale-notification-badge>
```

### Text

<Canvas withSource="none">
  <Story
    name="Label_Text"
    args={{
      type: 'text',
      label: '999+',
    }}
  >
    {TextTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-badge label="999+" type="text"
  >Service</scale-notification-badge
>
```
