import {
  Meta,
  Story,
  ArgsTable,
  Canvas,
  Description,
} from '@storybook/addon-docs/blocks';
import ScaleNotificationToast from './ScaleNotificationToast.vue';

<Meta
  title="Beta Components/Notification Toast"
  component={ScaleNotificationToast}
  argTypes={{
    variant: {
      defaultValue: 'informational',
      options: ['error', 'warning', 'success', 'informational'],
      control: {
        type: 'select',
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: {
    ...ScaleNotificationToast.props,
  },
  template: `<div style="height: 100px">
    <scale-notification-toast :story="story" :variant="variant"
      :opened="opened"
      ><p slot="header">Informational toast headline</p>
    </scale-notification-toast>
    </div>
  `,
});

export const TemplateText = (args, { argTypes }) => ({
  props: {
    ...ScaleNotificationToast.props,
  },
  template: `<div style="height: 100px">
    <scale-notification-toast :story="story" :variant="variant"
      :opened="opened"
      ><p slot="header">Informational toast headline</p>
      <p slot="body">Subheadline goes here.</p>
    </scale-notification-toast>
    </div>
  `,
});

<div
  style={{
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  }}
>
  <h1>Notification Toast</h1>
  <img src="assets/beta.png" alt="Beta Component" />
</div>

## Standard

<Canvas withSource="none">
  <Story
    name="Standard"
    args={{
      opened: true,
      story: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html

```

## Warning

<Canvas withSource="none">
  <Story
    name="Warning"
    args={{
      opened: true,
      story: true,
      variant: 'warning',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html

```

## Error

<Canvas withSource="none">
  <Story
    name="Error"
    args={{
      opened: true,
      story: true,
      variant: 'error',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html

```

## Success

<Canvas withSource="none">
  <Story
    name="Success"
    args={{
      opened: true,
      story: true,
      variant: 'success',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html

```

## With Text

<Canvas withSource="none">
  <Story
    name="With Text"
    args={{
      opened: true,
      story: true,
    }}
  >
    {TemplateText.bind({})}
  </Story>
</Canvas>

```html

```
