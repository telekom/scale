import { Meta, Description, DocsContainer } from "@storybook/addon-docs/blocks";

import text_en from "./09.Migration_Guide_to_V2_en.md";
import text_de from "./09.Migration_Guide_to_V2_de.md";

<Meta title="Scale for Developers/Migration Guide to v2"
  parameters={{
      docs: {
          container: ({ context }) => {
              const locale = context && context.globals && context.globals.locale
              return (
                  <DocsContainer context={context}>
                      {locale && <Description markdown={locale ==='en' ? text_en : text_de}/>}
                  </DocsContainer>
              )
          }
      },
  }}
/>
