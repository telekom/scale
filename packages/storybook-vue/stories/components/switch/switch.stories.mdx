import {
  Meta,
  Story,
  ArgsTable,
  Canvas,
  Description,
} from '@storybook/addon-docs';
import ScaleSwitch from './ScaleSwitch.vue';

<Meta title="Components/Switch" component={ScaleSwitch} />

export const Template = (args, { argTypes }) => ({
  components: { ScaleSwitch },
  props: ScaleSwitch.props,
  template: `
    <ScaleSwitch
      :disabled="disabled"
      :input-id="inputId"
      :checked="checked"
      :label="label"
      :size="size"
      :name="name"
      :styles="styles"
    >
    </ScaleSwitch>
  `
});

<div
  style={{
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  }}
>
  <h1>Switch</h1>
  <img src="assets/aa.png" alt="Accessible AA" />
</div>

## Standard

<Canvas withSource="none">
  <Story
    name="Standard"
    args={{
      label: 'Standard',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />


### Scoped CSS variables

```css
:host {
  --width: 42px;
  --height: 24px;
  --offset: 2px;
  --radius: 1em;
  --transition-duration: var(--telekom-motion-duration-immediate);
  --transition-easing: var(--telekom-motion-easing-standard);
  --shadow-thumb: 0 0 2px 0 rgba(0, 0, 0, 0.24), 0 2px 4px 0 rgba(0, 0, 0, 0.24),
    0 4px 12px 0 rgba(0, 0, 0, 0.26);
  --spacing-x-label: var(--telekom-spacing-composition-space-04);
  --font-label: var(--telekom-text-style-ui);
  --font-io-text: var(--telekom-text-style-small-bold);
  --color-label: var(--telekom-color-text-and-icon-standard);
}
```

## Small

<Canvas withSource="none">
  <Story
    name="Small"
    args={{
      label: 'Small',
      size: 'small',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-switch size="small" label="Standard disabled"></scale-switch>
```

## Standard disabled

<Canvas withSource="none">
  <Story
    name="Standard disabled"
    args={{
      disabled: true,
      label: 'Standard disabled',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-switch disabled label="Standard disabled"></scale-switch>
```

## Selected

<Canvas withSource="none">
  <Story
    name="Selected"
    args={{
      checked: true,
      label: 'Selected',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-switch checked label="Selected"></scale-switch>
```

## Selected disabled

<Canvas withSource="none">
  <Story
    name="Selected disabled"
    args={{
      disabled: true,
      checked: true,
      label: 'Selected disabled',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-switch disabled checked label="Selected disabled"></scale-switch>
```

## Android

<Canvas withSource="none">
  <Story
    name="Android"
    args={{
      label: 'Platform Android',
      name: 'android',
    }}
  >
    {(args, { argTypes }) => ({
      components: { ScaleSwitch },
      props: ScaleSwitch.props,
      template: `
        <div data-platform="android">
          <ScaleSwitch
            :label="label"
            :name="name"
            :disabled="disabled"
            :input-id="inputId"
            :checked="checked"
            :size="size"
            :styles="styles"
          >
          </ScaleSwitch>
        </div>
      `,
    })}
  </Story>
</Canvas>

```html
<div data-platform="android">
  <scale-switch label="Platform Android"></scale-switch>
</div>
```

## Android Disabled

<Canvas withSource="none">
  <Story
    name="Android Disabled"
    args={{
      disabled: true,
      label: 'Platform Android',
      name: 'android',
    }}
  >
    {(args, { argTypes }) => ({
      components: { ScaleSwitch },
      props: ScaleSwitch.props,
      template: `
        <div data-platform="android">
          <ScaleSwitch
            :label="label"
            :name="name"
            :disabled="disabled"
            :input-id="inputId"
            :checked="checked"
            :size="size"
            :styles="styles"
          >
          </ScaleSwitch>
        </div>
      `,
    })}
  </Story>
</Canvas>

```html
<div data-platform="android">
  <scale-switch disabled label="Platform Android"></scale-switch>
</div>
```

## Android Selected

<Canvas withSource="none">
  <Story
    name="Android Selected"
    args={{
      checked: true,
      label: 'Platform Android',
      name: 'android',
    }}
  >
    {(args, { argTypes }) => ({
      components: { ScaleSwitch },
      props: ScaleSwitch.props,
      template: `
        <div data-platform="android">
          <ScaleSwitch
            :label="label"
            :name="name"
            :disabled="disabled"
            :input-id="inputId"
            :checked="checked"
            :size="size"
            :styles="styles"
          >
          </ScaleSwitch>
        </div>
      `,
    })}
  </Story>
</Canvas>

```html
<div data-platform="android">
  <scale-switch checked label="Platform Android"></scale-switch>
</div>
```

## Android Selected Disabled

<Canvas withSource="none">
  <Story
    name="Android Selected Disabled"
    args={{
      disabled: true,
      checked: true,
      label: 'Platform Android',
      name: 'android',
    }}
  >
    {(args, { argTypes }) => ({
      components: { ScaleSwitch },
      props: ScaleSwitch.props,
      template: `
        <div data-platform="android">
          <ScaleSwitch
            :label="label"
            :name="name"
            :disabled="disabled"
            :input-id="inputId"
            :checked="checked"
            :size="size"
            :styles="styles"
          >
          </ScaleSwitch>
        </div>
      `,
    })}
  </Story>
</Canvas>

```html
<div data-platform="android">
  <scale-switch disabled checked label="Platform Android"></scale-switch>
</div>
```
