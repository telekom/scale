import {
  Meta,
  Story,
  ArgsTable,
  Canvas,
  Description,
} from '@storybook/addon-docs';
import ScaleNotificationMessage from './ScaleNotificationMessage.vue';

<Meta
  title="Beta Components/Notification Message"
  component={ScaleNotificationMessage}
  argTypes={{
    variant: {
      defaultValue: 'informational',
      options: ['error', 'warning', 'success', 'informational'],
      control: {
        type: 'select',
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: {
    ...ScaleNotificationMessage.props,
    heading: String,
    text: String,
  },
  template: `
    <scale-notification-message   
      :variant="variant"
      :dismissible="dismissible"
      :auto-hide-duration="autoHideDuration"
      :auto-hide="autoHide"
      :opened="opened"
    >
      {{ heading }} 
      <p v-if="text">{{ text }}</p>
    </scale-notification-message>
  `,
});

<div
  style={{
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  }}
>
  <h1>Notification Message</h1>
  <img src="assets/beta.png" alt="Beta Component" />
</div>

## Standard

<Canvas withSource="none">
  <Story
    name="Standard"
    args={{
      opened: true,
      heading: 'Notification text goes here',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-notification-message opened
  >Notification text goes here</scale-notification-message
>
```

## Scoped CSS variables

```css
:host {
  --width: 100%;
  --radius: var(--telekom-radius-standard);
  --border: var(--telekom-line-weight-standard) solid transparent;

  --background-error: var(--telekom-color-functional-danger-subtle);
  --background-warning: var(--telekom-color-functional-warning-subtle);
  --background-informational: var(
    --telekom-color-functional-informational-subtle
  );
  --background-success: var(--telekom-color-functional-success-subtle);
}
```

## Variants

### Warning

<Canvas withSource="none">
  <Story
    name="Warning"
    args={{
      opened: true,
      variant: 'warning',
      heading: 'Notification text goes here',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-message variant="warning" opened>
  Notification text goes here
</scale-notification-message>
```

### Error

<Canvas withSource="none">
  <Story
    name="Error"
    args={{
      opened: true,
      variant: 'error',
      heading: 'Notification text goes here',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-message variant="error" opened>
  Notification text goes here
</scale-notification-message>
```

### Success

<Canvas withSource="none">
  <Story
    name="Success"
    args={{
      opened: true,
      variant: 'success',
      heading: 'Notification text goes here',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-message variant="success" opened>
  Notification text goes here
</scale-notification-message>
```

### Informational

<Canvas withSource="none">
  <Story
    name="Informational"
    args={{
      opened: true,
      variant: 'informational',
      heading: 'Notification text goes here',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-message variant="informational" opened>
  Notification text goes here
</scale-notification-message>
```

## Dismissible

<Canvas withSource="none">
  <Story
    name="Dismissible"
    args={{
      opened: true,
      dismissible: true,
      heading: 'Notification text goes here',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-message dismissible opened>
  Notification text goes here
</scale-notification-message>
```

## Timeout

<Canvas withSource="none">
  <Story
    name="Timeout"
    args={{
      opened: true,
      autoHideDuration: 4000,
      autoHide: true,
      heading: 'Notification text goes here',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-message auto-hide-duration="4000" auto-hide opened>
  Notification text goes here
</scale-notification-message>
```

## With Text

<Canvas withSource="none">
  <Story
    name="With Text"
    args={{
      opened: true,
      heading: 'Notification text goes here',
      text: 'Here you can add some more text',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-message opened>
  Notification text goes here.
  <p slot="text">Here you can add some more text.</p>
</scale-notification-message>
```
