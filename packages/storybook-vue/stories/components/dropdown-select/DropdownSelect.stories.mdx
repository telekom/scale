import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs';
import ScaleDropdownSelect from './ScaleDropdownSelect.vue';
import { action } from '@storybook/addon-actions';

<Meta
  title="Beta Components/Dropdown Select"
  component={ScaleDropdownSelect}
  argTypes={{}}
/>

export const Template = (args, { argTypes }) => ({
  props: { ...ScaleDropdownSelect.props },
  template: `
    <div style="min-height: 350px;">
      <scale-dropdown-select
        :label="label"
        :helper-text="helperText"
        :invalid="invalid"
        :variant="variant"
        :disabled="disabled"
        :size="size"
        :value="value"
        :combobox-id="comboboxId"
        @scale-change="handleScaleChange"
        @scale-focus="handleScaleFocus"
        @scale-blur="handleScaleBlur"
        @scale-keydown="handleScaleKeydown"
      >
        <scale-dropdown-select-item value="caspar">Caspar</scale-dropdown-select-item>
        <scale-dropdown-select-item value="cedric">Cedric</scale-dropdown-select-item>
        <scale-dropdown-select-item value="cem">Cem</scale-dropdown-select-item>
      </scale-dropdown-select>
    </div>
  `,
  methods: {
    handleScaleChange: action('scale-change'),
    handleScaleFocus: action('scale-focus'),
    handleScaleBlur: action('scale-blur'),
    handleScaleKeydown: action('scale-keydown'),
  },
});

<div
  style={{
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  }}
>
  <h1>Dropdown Select</h1>
  <img src="assets/beta.png" alt="Beta Component" />
</div>

## Standard

<Canvas withSource="none">
  <Story
    name="Standard"
    args={{
      label: 'Select',
      value: 'caspar',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-dropdown-select label="Select" value="caspar">
  <scale-dropdown-select-item value="caspar">Caspar</scale-dropdown-select-item>
  <scale-dropdown-select-item value="cedric">Cedric</scale-dropdown-select-item>
  <scale-dropdown-select-item value="cem">Cem</scale-dropdown-select-item>
</scale-dropdown-select>
```

## Disabled

<Canvas withSource="none">
  <Story
    name="Disabled"
    args={{
      label: 'Select',
      value: 'cedric',
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-dropdown-select label="Select" value="cedric" disabled>
  <scale-dropdown-select-item value="caspar">Caspar</scale-dropdown-select-item>
  <scale-dropdown-select-item value="cedric">Cedric</scale-dropdown-select-item>
  <scale-dropdown-select-item value="cem">Cem</scale-dropdown-select-item>
</scale-dropdown-select>
```

## Error

<Canvas withSource="none">
  <Story
    name="Error"
    args={{
      label: 'Select',
      value: 'cem',
      invalid: true,
      helperText: 'Please select a different option',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-dropdown-select label="Select" value="cem" invalid helper-text="Please select a different option">
  <scale-dropdown-select-item value="caspar">Caspar</scale-dropdown-select-item>
  <scale-dropdown-select-item value="cedric">Cedric</scale-dropdown-select-item>
  <scale-dropdown-select-item value="cem">Cem</scale-dropdown-select-item>
</scale-dropdown-select>
```
