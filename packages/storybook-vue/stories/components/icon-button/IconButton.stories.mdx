import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs';
import ScaleIconButton from './ScaleIconButton';

<Meta
  title="Beta Components/Icon Button"
  component={ScaleIconButton}
  argTypes={{
    type: {
      options: ['standard', 'activate', 'toggle'],
      defaultValue: 'standard',
      control: {
        type: 'select',
      },
    },
    label: {
      defaultValue: 'Label',
      control: {
        type: 'text',
      },
    }, 
    labelPlacement: {
      options: [ 'right', 'bottom'],
      defaultValue: 'right',
      control: {
        type: 'select',
      },
    },
    size: {
      options: [ 'small', 'standard', 'large'],
      defaultValue: 'standard',
      control: {
        type: 'select',
      },
    },
        
  }}  
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleIconButton },
  props: {
    label: String,
    icon: String,
    ...ScaleIconButton.props,
  },
  template: `
    <scale-icon-button
      :disabled="disabled"
      :active="active"
      :type="type"
      :label="label"
      :size="size"
      :label-placement="labelPlacement"
    >
      <scale-icon-alert-help accessibility-title="help" size="24"></scale-icon-alert-help>
    </scale-icon-button>
  `,
});

<div
  style={{
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  }}
>
  <h1>Icon Button</h1>
  <img src="assets/beta.png" alt="Beta Component" />
</div>


## Standard

<Canvas withSource="none">
  <Story name="Standard" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

## Toggle

<Canvas withSource="none">
  <Story name="Toggle" args={{}}>
    {() => ({
      template: `
        <scale-icon-button type="toggle" size="standard">
          <scale-icon-alert-help accessibility-title="help" size="20"></scale-icon-alert-help>
        </scale-icon-button>
      `,
    })}
  </Story>
</Canvas>

```html
<scale-icon-button size="standard">
  <scale-icon-alert-help
    accessibility-title="help"
    size="20"
  ></scale-icon-alert-help>
</scale-icon-button>
```

## Activate

<Canvas withSource="none">
  <Story name="Activate" args={{}}>
    {() => ({
      template: `
        <scale-icon-button size="standard" type="activate">
          <scale-icon-alert-help accessibility-title="help" size="20"></scale-icon-alert-help>
        </scale-icon-button>
      `,
    })}
  </Story>
</Canvas>

```html
<scale-icon-button size="standard" active="true">
  <scale-icon-alert-help
    accessibility-title="help"
    size="20"
  ></scale-icon-alert-help>
</scale-icon-button>
```