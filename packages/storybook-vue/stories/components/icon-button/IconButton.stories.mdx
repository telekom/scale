import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs';
import ScaleIconButton from './ScaleIconButton';

<Meta
  title="Beta Components/Icon Button"
  component={ScaleIconButton}
  argTypes={{
    type: {
      options: ['standard', 'activate', 'toggle'],
      defaultValue: 'standard',
      control: {
        type: 'select',
      },
    },
    label: {
      defaultValue: 'asdf',
      control: {
        type: 'text',
      },
    }, 
    labelPlacement: {
      options: [ 'right', 'bottom'],
      defaultValue: 'bottom',
      control: {
        type: 'select',
      },
    },
  }}  
/>

<div
  style={{
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  }}
>
  <h1>Callout</h1>
  <img src="assets/beta.png" alt="Beta Component" />
</div>


## Standard

<Canvas withSource="none">
  <Story name="Standard" args={{}}>
    {() => ({
      template: `
        <scale-icon-button
          :disabled="disabled"
          :active="active"
          :type="type"
          :label="label"
          :size="size"
          :labelPlacement="labelPlacement"
          @scaleChange="scaleChange"        
        >
          <scale-icon-alert-help accessibility-title="help" size="20"></scale-icon-alert-help>
        </scale-icon-button>
      `,
    })}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

## Toggle

<Canvas withSource="none">
  <Story name="Selected" args={{}}>
    {() => ({
      template: `
        <scale-icon-button type="toggle" size="medium">
          <scale-icon-alert-help accessibility-title="help" size="20"></scale-icon-alert-help>
        </scale-icon-button>
      `,
    })}
  </Story>
</Canvas>

```html
<scale-icon-button size="medium">
  <scale-icon-alert-help
    accessibility-title="help"
    size="20"
  ></scale-icon-alert-help>
</scale-icon-button>
```

## Activate

<Canvas withSource="none">
  <Story name="Active" args={{}}>
    {() => ({
      template: `
        <scale-icon-button size="medium" type="activate">
          <scale-icon-alert-help accessibility-title="help" size="20"></scale-icon-alert-help>
        </scale-icon-button>
      `,
    })}
  </Story>
</Canvas>

```html
<scale-icon-button size="medium" active="true">
  <scale-icon-alert-help
    accessibility-title="help"
    size="20"
  ></scale-icon-alert-help>
</scale-icon-button>
```

## Active Selected

<Canvas withSource="none">
  <Story name="Active Selected" args={{}}>
    {() => ({
      template: `
        <scale-icon-button size="medium" active="true">
          <scale-icon-alert-help accessibility-title="help" size="20"></scale-icon-alert-help>
        </scale-icon-button>
      `,
    })}
  </Story>
</Canvas>

```html
<scale-icon-button size="medium" active="true">
  <scale-icon-alert-help
    accessibility-title="help"
    size="20"
    selected
  ></scale-icon-alert-help>
</scale-icon-button>
```
