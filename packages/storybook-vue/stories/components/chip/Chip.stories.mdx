import {
  Meta,
  Story,
  ArgsTable,
  Canvas,
  Description,
} from '@storybook/addon-docs';
import ScaleChip from './ScaleChip.vue';
import { action } from '@storybook/addon-actions';

<Meta
  title="Beta Components/Chip"
  component={ScaleChip}
  argTypes={{
    label: {
      control: { type: 'text' },
    },
    type: {
      control: {
        type: 'select',
        options: ['standard', 'outline'],
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: {
    label: String,
    ...ScaleChip.props,
  },
  template: `
        <scale-chip
            :type="type"
            :selected="selected"
            :dismissible="dismissible"
            :label="label"
            :disabled="disabled"
            :styles="styles"
        >
        </scale-chip>
  `,
  methods: {
    action,
  },
});

export const TemplateIcon = (args, { argTypes }) => ({
  props: {
    label: String,
    ...ScaleChip.props,
  },
  template: `
    <scale-chip
      :selected="selected"
      :dismissible="dismissible"
      :label="label"
      :disabled="disabled"
      :styles="styles"
      >
      <div slot="chip-icon">
        <scale-icon-action-favorite accessibility-title="favorite" />
      </div>
    </scale-chip>
  `,
  methods: {
    action,
  },
});

<div
  style={{
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  }}
>
  <h1>Chip</h1>
  <img src="assets/beta.png" alt="Beta Component" />
</div>

## Standard

<Canvas withSource="none">
  <Story
    name="Standard"
    args={{
      label: 'Label',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-chip label="Label"></scale-chip>
```

### Standard Selected

<Canvas withSource="none">
  <Story
    name="Standard Selected"
    args={{
      label: 'Label',
      selected: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip label="Label" selected></scale-chip>
```

### Standard Disabled

<Canvas withSource="none">
  <Story
    name="Standard Disabled"
    args={{
      label: 'Label',
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip disabled label="Label" selected></scale-chip>
```

### Standard Selected Disabled

<Canvas withSource="none">
  <Story
    name="Standard Selected Disabled"
    args={{
      label: 'Label',
      disabled: true,
      selected: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip disabled label="Label" selected></scale-chip>
```

## Outline

<Canvas withSource="none">
  <Story
    name="Outline"
    args={{
      label: 'Label',
      type: 'outline',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip label="Label" type="outline"></scale-chip>
```

### Outline Selected

<Canvas withSource="none">
  <Story
    name="Outline Selected"
    args={{
      label: 'Label',
      type: 'outline',
      selected: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip label="Label" selected></scale-chip>
```

### Outline Disabled

<Canvas withSource="none">
  <Story
    name="Outline Disabled"
    args={{
      type: 'outlined',
      label: 'Label',
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip disabled type="outline" label="Label" selected></scale-chip>
```

### Outline Selected Disabled

<Canvas withSource="none">
  <Story
    name="Outline Selected Disabled"
    args={{
      type: 'outlined',
      label: 'Label',
      disabled: true,
      selected: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip disabled type="outline" label="Label" selected></scale-chip>
```

## Dismissible

### Standard Dismissible

<Canvas withSource="none">
  <Story
    name="Standard Dismissible"
    args={{
      label: 'Label',
      dismissible: true,
      selected: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip dismissible selected label="Label"></scale-chip>
```

### Outline Dismissible

<Canvas withSource="none">
  <Story
    name="Outline Dismissible"
    args={{
      label: 'Label',
      dismissible: true,
      type: 'outline',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip dismissible selected label="Label" type="outline"></scale-chip>
```

## Icon + Text

<Canvas withSource="none">
  <Story
    name="Icon+Text"
    args={{
      label: 'Label',
    }}
  >
    {TemplateIcon.bind({})}
  </Story>
</Canvas>

```html
<scale-chip label="Label"
  ><div slot="chip-icon">
    <scale-icon-action-favorite accessibility-title="favorite" />
  </div>
</scale-chip>
```
