import {
  Meta,
  Story,
  ArgsTable,
  Canvas,
  Description,
} from '@storybook/addon-docs';
import ScaleChip from './ScaleChip.vue';
import { action } from '@storybook/addon-actions';

<Meta
  title="Beta Components/Chip"
  component={ScaleChip}
  argTypes={{
    label: {
      control: { type: 'text' },
    },
    type: {
      control: {
        type: 'select',
        options: ['standard', 'outline'],
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: {
    label: String,
    ...ScaleChip.props,
  },
  template: `
    <template>
        <scale-chip
            :type="type"
            :selected="selected"
            :dismissible="dismissible"
            :label="label"
            :disabled="disabled"
            :styles="styles"
            :icon-size="iconSize"
        >
        </scale-chip>
    </template>
  `,
  methods: {
    action,
  },
});

export const TemplateIcon = (args, { argTypes }) => ({
  props: {
    label: String,
    ...ScaleChip.props,
  },
  template: `
    <template>
        <scale-chip
            :selected="selected"
            :dismissible="dismissible"
            :label="label"
            :disabled="disabled"
            :styles="styles"
            :icon-size="iconSize"
        >
        <div slot="chip-icon">
            <scale-icon-action-favorite accessibility-title="favorite" />
        </div>
        </scale-chip>
    </template>
  `,
  methods: {
    action,
  },
});

<div
  style={{
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  }}
>
  <h1>Chip</h1>
  <img src="assets/beta.png" alt="Beta Component" />
</div>

## Standard

<Canvas withSource="none">
  <Story
    name="Standard"
    args={{
      label: 'Standard Chip',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-chip label="Standard Chip"></scale-chip>
```

### Standard Selected

<Canvas withSource="none">
  <Story
    name="Standard Selected"
    args={{
      label: 'Standard Selected Chip',
      selected: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip label="Standard Selected Chip" selected></scale-chip>
```

### Standard Selected Disabled

<Canvas withSource="none">
  <Story
    name="Standard Selected Disabled Chip"
    args={{
      label: 'Standard Selected Disabled Chip',
      disabled: true,
      selected: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip
  disabled
  label="Standard Selected Disabled Chip"
  selected
></scale-chip>
```

### Standard Dismissible

<Canvas withSource="none">
  <Story
    name="Standard Dismissible Chip"
    args={{
      label: 'Standard Dismissible Chip',
      dismissible: true,
      selected: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip dismissible selected label="Standard Dismissible Chip"></scale-chip>
```

## Outline

<Canvas withSource="none">
  <Story
    name="Outline"
    args={{
      label: 'Outline Chip',
      type: 'outline',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip label="Standard Chip" type="outline"></scale-chip>
```

### Outline Selected

<Canvas withSource="none">
  <Story
    name="Outline Selected"
    args={{
      label: 'Outline Selected Chip',
      type: 'outline',
      selected: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip label="Outline Selected Chip" selected></scale-chip>
```

### Outline Selected Disabled

<Canvas withSource="none">
  <Story
    name="Outline Selected Disabled Chip"
    args={{
      type: 'outlined',
      label: 'Outline Selected Disabled Chip',
      disabled: true,
      selected: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip
  disabled
  type="otline"
  label="Outline Selected Disabled Chip"
  selected
></scale-chip>
```

### Outline Dismissible

<Canvas withSource="none">
  <Story
    name="Outline Dismissible Chip"
    args={{
      label: 'Outline Dismissible Chip',
      dismissible: true,
      selected: true,
      type: 'outline',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip
  dismissible
  selected
  label="Standard Dismissible Chip"
  type="outline"
></scale-chip>
```

## Icon

<Canvas withSource="none">
  <Story
    name="Icon Chip"
    args={{
      label: 'Icon Chip',
    }}
  >
    {TemplateIcon.bind({})}
  </Story>
</Canvas>

```html
<scale-chip label="Icon Chip"
  ><div slot="chip-icon">
    <scale-icon-action-favorite accessibility-title="favorite" />
  </div>
</scale-chip>
```
