import {
  Meta,
  Story,
  ArgsTable,
  Canvas,
  Description,
} from '@storybook/addon-docs';
import ScaleChip from './ScaleChip.vue';
import { action } from '@storybook/addon-actions';

<Meta
  title="Beta Components/Chip"
  component={ScaleChip}
  argTypes={{
    label: {
      control: { type: 'text' },
    },
    type: {
      control: {
        type: 'select',
        options: ['standard', 'outline'],
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: {
    label: String,
    ...ScaleChip.props,
  },
  template: `
        <scale-chip
            :type="type"
            :selected="selected"
            :dismissible="dismissible"
            :label="label"
            :disabled="disabled"
            :styles="styles"
            :icon-size="iconSize"
        >
        </scale-chip>
  `,
  methods: {
    action,
  },
});

export const TemplateIcon = (args, { argTypes }) => ({
  props: {
    label: String,
    ...ScaleChip.props,
  },
  template: `
    <scale-chip
      :selected="selected"
      :dismissible="dismissible"
      :label="label"
      :disabled="disabled"
      :styles="styles"
      :icon-size="iconSize"
      >
      <div slot="chip-icon">
        <scale-icon-action-favorite accessibility-title="favorite" />
      </div>
    </scale-chip>
  `,
  methods: {
    action,
  },
});

<div
  style={{
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  }}
>
  <h1>Chip</h1>
  <img src="assets/beta.png" alt="Beta Component" />
</div>

## Standard

<Canvas withSource="none">
  <Story
    name="Standard"
    args={{
      label: 'Standard',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-chip label="Standard"></scale-chip>
```

### Standard Selected

<Canvas withSource="none">
  <Story
    name="Standard Selected"
    args={{
      label: 'Standard Selected',
      selected: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip label="Standard Selected" selected></scale-chip>
```

### Standard Selected Disabled

<Canvas withSource="none">
  <Story
    name="Standard Selected Disabled"
    args={{
      label: 'Standard Selected Disabled',
      disabled: true,
      selected: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip disabled label="Standard Selected Disabled" selected></scale-chip>
```

### Standard Dismissible

<Canvas withSource="none">
  <Story
    name="Standard Dismissible"
    args={{
      label: 'Standard Dismissible',
      dismissible: true,
      selected: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip dismissible selected label="Standard Dismissible"></scale-chip>
```

## Outline

<Canvas withSource="none">
  <Story
    name="Outline"
    args={{
      label: 'Outline',
      type: 'outline',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip label="Outline" type="outline"></scale-chip>
```

### Outline Selected

<Canvas withSource="none">
  <Story
    name="Outline Selected"
    args={{
      label: 'Outline Selected',
      type: 'outline',
      selected: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip label="Outline Selected" selected></scale-chip>
```

### Outline Selected Disabled

<Canvas withSource="none">
  <Story
    name="Outline Selected Disabled"
    args={{
      type: 'outlined',
      label: 'Outline Selected Disabled',
      disabled: true,
      selected: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip
  disabled
  type="otline"
  label="Outline Selected Disabled"
  selected
></scale-chip>
```

### Outline Dismissible

<Canvas withSource="none">
  <Story
    name="Outline Dismissible"
    args={{
      label: 'Outline Dismissible',
      dismissible: true,
      selected: true,
      type: 'outline',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-chip
  dismissible
  selected
  label="Standard Dismissible"
  type="outline"
></scale-chip>
```

## Icon

<Canvas withSource="none">
  <Story
    name="Icon"
    args={{
      label: 'Icon',
    }}
  >
    {TemplateIcon.bind({})}
  </Story>
</Canvas>

```html
<scale-chip label="Icon"
  ><div slot="chip-icon">
    <scale-icon-action-favorite accessibility-title="favorite" />
  </div>
</scale-chip>
```
