import { Meta, Description, DocsContainer } from '@storybook/addon-docs/blocks';
import index from '../color_table.json';

import text_en from './5.Colors_en.md';
import text_de from './5.Colors_de.md';

<Meta
  title="Scale for Designers/Colors"
  parameters={{
    docs: {
      container: ({ context }) => {
        const locale = context && context.globals && context.globals.locale;
        let newSection = false;
        let colorName;
        let colorPath;
        let first;
        let pathHeaderOutput = false;
        return (
          <DocsContainer context={context}>
            <table>
              {index.map((item, i) => {
                if (i !== 0) {
                  if (index[i - 1].section !== index[i].section) {
                    newSection = true;
                  } else {
                    newSection = false;
                  }
                } else {
                  newSection = true;
                }
                return (
                  <div>
                    {newSection ? (
                      <thead>
                        <h3 style={{ paddingTop: '200px' }}>
                          {index[i].section}
                        </h3>
                        {index.map((item) => {
                          colorName = item.name;
                          colorPath = colorName.split('/');
                          if (first !== colorPath[1]) {
                            first = colorPath[1];
                            pathHeaderOutput = true;
                          } else {
                            pathHeaderOutput = false;
                          }
                          return index[i].section == item.section ? (
                            <div style={{ backgroundColor: '#f0f1f1' }}>
                              {pathHeaderOutput ? <div> {first} </div> : null}
                              <a href={`#${item.cssVariableName}`}>
                                <div
                                  style={{
                                    color: `${item.value}`,
                                    fontWeight: 'bold',
                                  }}
                                >
                                  {colorPath[2]
                                    ? colorPath[1] + colorPath[2]
                                    : colorPath[1]}
                                </div>
                              </a>
                            </div>
                          ) : null;
                        })}
                        <tr>
                          <th
                            style={{
                              textAlign: 'left',
                              color: '#6A6A6A',
                              fontSize: '13px',
                            }}
                          >
                            Value
                          </th>
                          <th
                            style={{
                              textAlign: 'left',
                              color: '#6A6A6A',
                              fontSize: '13px',
                            }}
                          >
                            Comment
                          </th>
                          <th
                            style={{
                              textAlign: 'left',
                              color: '#6A6A6A',
                              fontSize: '13px',
                            }}
                          >
                            CSS-Variable Name
                          </th>
                        </tr>
                      </thead>
                    ) : null}
                    <tbody>
                      <td>
                        <tr style={{ height: '150px' }}>
                          <div
                            style={{
                              width: '400px',
                              marginTop: '7px',
                              marginBottom: '7px',
                            }}
                          >
                            <div
                              style={{
                                height: '100px',
                                width: '350px',
                                borderRadius: '2px',
                                backgroundColor: `${item.value}`,
                                marginBottom: '10px',
                                fontSize: '8px',
                              }}
                            ></div>
                            <span
                              style={{
                                fontSize: '14px',
                              }}
                            >
                              {item.value}
                            </span>
                          </div>
                        </tr>
                      </td>
                      <td>
                        <tr style={{ height: '150px' }}>
                          <div
                            style={{
                              marginRight: '100px',
                              marginTop: '7px',
                              marginBottom: '7px',
                            }}
                          >
                            <div
                              style={{
                                width: '200px',
                              }}
                            >
                              <span style={{ fontWeight: 'bold' }}>
                                {item.name}
                              </span>
                            </div>
                            <div>
                              <span>{item.comment}</span>
                            </div>
                          </div>
                        </tr>
                      </td>
                      <td>
                        <tr style={{ height: '150px' }}>
                          <div
                            style={{
                              marginLeft: '20px',
                              marginRight: '100px',
                              marginTop: '30px',
                              marginBottom: '30px',
                              width: '450px',
                              height: '50px',
                              backgroundColor: '#D9DBDE',
                              textAlign: 'start',
                              position: 'relative',
                            }}
                          >
                            <span
                              style={{
                                fontSize: '12px',
                                lineHeight: '50px',
                                padding: '30px',
                              }}
                              id={item.cssVariableName}
                            >
                              {item.cssVariableName}
                            </span>
                            <button
                              style={{
                                position: 'absolute',
                                padding: '4px',
                                top: '5px',
                                right: '5px',
                              }}
                              onClick={() => {
                                navigator.clipboard.writeText(
                                  item.cssVariableName
                                );
                              }}
                            >
                              <scale-icon-action-copy-paste accessibility-title="copy-paste" />
                            </button>
                          </div>
                        </tr>
                      </td>
                    </tbody>
                  </div>
                );
              })}
            </table>
          </DocsContainer>
        );
      },
    },
  }}
/>
