import {
  Meta,
  Story,
  ArgsTable,
  Canvas,
  Description,
} from '@storybook/addon-docs';
import ScaleNotificationBanner from './ScaleNotificationBanner.vue';

<Meta
  title="Deprecated Components/Notification Banner"
  component={ScaleNotificationBanner}
  argTypes={{
    variant: {
      defaultValue: 'informational',
      options: ['error', 'warning', 'success', 'informational'],
      control: {
        type: 'select',
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: {
    ...ScaleNotificationBanner.props,
    heading: String,
    text: String,
    link: String,
  },
  template: `
    <scale-notification-banner   
      :variant="variant"
      :dismissible="dismissible"
      :auto-hide-duration="autoHideDuration"
      :auto-hide="autoHide"
      :opened="opened"
      :close-button-label="closeButtonLabel"
      :close-button-title="closeButtonTitle"
    >
      {{ heading }} 
      <p v-if="text">{{ text }}</p>
      <p v-if="link">{{ link }}</p>
    </scale-notification-banner>
  `,
});

export const TextTemplate = (args, { argTypes }) => ({
  props: {
    ...ScaleNotificationBanner.props,
  },
  template: `
    <scale-notification-banner opened>
      Notification text goes here.
      <p slot="text">Here you can add some more text.</p>
    </scale-notification-banner>
  `,
});

export const LinkTemplate = (args, { argTypes }) => ({
  props: {
    ...ScaleNotificationBanner.props,
    text: String,
  },
  template: `
    <scale-notification-banner opened>
      Notification text goes here.
      <p v-if="text">{{ text }}</p>
      <p slot="link">Link text</p>
    </scale-notification-banner>
  `,
});

export const LinkTextTemplate = (args, { argTypes }) => ({
  props: {
    ...ScaleNotificationBanner.props,
    text: String,
  },
  template: `
    <scale-notification-banner opened>
      Notification text goes here.
      <p slot="text">Here you can add some more text</p>
      <p slot="link">Link text</p>
    </scale-notification-banner>
  `,
});

<div
  style={{
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  }}
>
  <h1>Notification Banner</h1>
  <img src="assets/deprecated.png" alt="Deprecated Component" />
</div>

## Standard

<Canvas withSource="none">
  <Story
    name="Standard"
    args={{
      opened: true,
      heading: 'Notification text goes here',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-notification-banner opened
  >Notification text goes here</scale-notification-banner
>
```

## Scoped CSS variables

```css
:host {
  --width: 100%;
  --radius: var(--telekom-radius-standard);

  --background-error: var(--telekom-color-functional-danger-subtle);
  --background-warning: var(--telekom-color-functional-warning-subtle);
  --background-informational: var(
    --telekom-color-functional-informational-subtle
  );
  --background-success: var(--telekom-color-functional-success-subtle);
}
```

## Variants

### Warning

<Canvas withSource="none">
  <Story
    name="Warning"
    args={{
      opened: true,
      variant: 'warning',
      heading: 'Notification text goes here',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-banner variant="warning" opened>
  Notification text goes here
</scale-notification-banner>
```

### Error

<Canvas withSource="none">
  <Story
    name="Error"
    args={{
      opened: true,
      variant: 'error',
      heading: 'Notification text goes here',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-banner variant="error" opened>
  Notification text goes here
</scale-notification-banner>
```

### Success

<Canvas withSource="none">
  <Story
    name="Success"
    args={{
      opened: true,
      variant: 'success',
      heading: 'Notification text goes here',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-banner variant="success" opened>
  Notification text goes here
</scale-notification-banner>
```

### Informational

<Canvas withSource="none">
  <Story
    name="Informational"
    args={{
      opened: true,
      variant: 'informational',
      heading: 'Notification text goes here',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-banner variant="informational" opened>
  Notification text goes here
</scale-notification-banner>
```

## Dismissible

<Canvas withSource="none">
  <Story
    name="Dismissible"
    args={{
      opened: true,
      dismissible: true,
      heading: 'Notification text goes here',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-banner dismissible opened>
  Notification text goes here
</scale-notification-banner>
```

## Timeout

<Canvas withSource="none">
  <Story
    name="Timeout"
    args={{
      opened: true,
      autoHideDuration: 4000,
      autoHide: true,
      heading: 'Notification text goes here',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-banner auto-hide auto-hide-duration="4000" opened>
  Notification text goes here
</scale-notification-banner>
```

## With Text

<Canvas withSource="none">
  <Story
    name="With Text"
    args={{
      opened: true,
    }}
  >
    {TextTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-banner opened>
  Notification text goes here.
  <p slot="text">Here you can add some more text.</p>
</scale-notification-banner>
```

## With Link

<Canvas withSource="none">
  <Story
    name="With Link"
    args={{
      opened: true,
    }}
  >
    {LinkTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-banner opened>
  Notification text goes here.
  <p slot="link">Link Text</p>
</scale-notification-banner>
```

## With Link and Text

<Canvas withSource="none">
  <Story
    name="With Link and Text"
    args={{
      opened: true,
    }}
  >
    {LinkTextTemplate.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-banner opened>
  Notification text goes here.
  <p slot="text">Here you can add some more text.</p>
  <p slot="link">Link Text</p>
</scale-notification-banner>
```
