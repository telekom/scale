import {
  Meta,
  Story,
  ArgsTable,
  Canvas,
  Description,
} from '@storybook/addon-docs';
import ScaleNotificationToast from './ScaleNotificationToast.vue';

<Meta
  title="Deprecated Components/Notification Toast"
  component={ScaleNotificationToast}
  argTypes={{
    variant: {
      defaultValue: 'informational',
      options: ['error', 'warning', 'success', 'informational'],
      control: {
        type: 'select',
      },
    },
    alignment: {
      defaultValue: 'top-right',
      options: ['bottom-right', 'bottom-left', 'top-right', 'top-left'],
      control: {
        type: 'select',
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: {
    ...ScaleNotificationToast.props,
  },
  template: `<div style="height: 100px">
    <scale-notification-toast 
      story
      :position-vertical="positionVertical"
      :position-horizontal="positionHorizontal"
      :variant="variant" 
      :alignment="alignment"
      :opened="opened"
      :auto-hide-duration="autoHideDuration"
      :auto-hide="autoHide"
      :close-button-label="closeButtonLabel"
      :close-button-title="closeButtonTitle"
      ><p slot="header">Informational toast headline</p>
    </scale-notification-toast>
    </div>
  `,
});

export const TemplateText = (args, { argTypes }) => ({
  props: {
    ...ScaleNotificationToast.props,
  },
  template: `<div style="height: 100px">
    <scale-notification-toast 
      story
      :variant="variant"
      :opened="opened"
      ><p slot="header">Informational toast headline</p>
      <p slot="body">Subheadline goes here.</p>
    </scale-notification-toast>
    </div>
  `,
});

export const TemplateLink = (args, { argTypes }) => ({
  props: {
    ...ScaleNotificationToast.props,
  },
  template: `<div style="height: 100px">
    <scale-notification-toast 
      story 
      :variant="variant"
      :opened="opened"
      ><p slot="header">Informational toast headline</p>
      <p slot="link">Link Text</p>
    </scale-notification-toast>
    </div>
  `,
});

<div
  style={{
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  }}
>
  <h1>Notification Toast</h1>
  <img src="assets/deprecated.png" alt="Deprecated Component" />
</div>

## Standard

<Canvas withSource="none">
  <Story
    name="Standard"
    args={{
      opened: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-notification-toast opened>
  <p slot="header">Informational toast headline</p>
</scale-notification-toast>
```

## Warning

<Canvas withSource="none">
  <Story
    name="Warning"
    args={{
      opened: true,
      variant: 'warning',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-toast variant="warning" opened>
  <p slot="header">Informational toast headline</p>
</scale-notification-toast>
```

## Error

<Canvas withSource="none">
  <Story
    name="Error"
    args={{
      opened: true,
      variant: 'error',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-toast variant="error" opened>
  <p slot="header">Informational toast headline</p>
</scale-notification-toast>
```

## Success

<Canvas withSource="none">
  <Story
    name="Success"
    args={{
      opened: true,
      variant: 'success',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-toast variant="success" opened>
  <p slot="header">Informational toast headline</p>
</scale-notification-toast>
```

## Informational

<Canvas withSource="none">
  <Story
    name="Informational"
    args={{
      opened: true,
      variant: 'informational',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-toast variant="informational" opened>
  <p slot="header">Informational toast headline</p>
</scale-notification-toast>
```

## Timeout

<Canvas withSource="none">
  <Story
    name="Timeout"
    args={{
      opened: true,
      autoHide: true,
      autHideDuration: '4000',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-toast opened>
  <p slot="header" auto-hide auto-hide-duration="4000">
    Informational toast headline
  </p>
</scale-notification-toast>
```

## With Text

<Canvas withSource="none">
  <Story
    name="With Text"
    args={{
      opened: true,
    }}
  >
    {TemplateText.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-toast>
  <p slot="header">Informational toast headline</p>
  <p slot="body">Subheadline goes here.</p>
</scale-notification-toast>
```

## With Link

<Canvas withSource="none">
  <Story
    name="With Link"
    args={{
      opened: true,
    }}
  >
    {TemplateLink.bind({})}
  </Story>
</Canvas>

```html
<scale-notification-toast variant="success" opened>
  <p slot="header">Informational toast headline</p>
  <p slot="link">Link Text</p>
</scale-notification-toast>
```
